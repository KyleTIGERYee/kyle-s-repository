## 任务概述
在所有页面中只展示第一战区到第六战区及其下属门店的数据，所有数据都按这个逻辑进行筛选。

## 需要修改的文件

### 1. `services/dataAdapter.ts`
**修改位置**: 第 82-92 行的 `WAR_ZONE_TO_REGION_ID` 映射表
**修改内容**: 
- 移除 `'职能部门': 'R0'` 条目
- 添加一个允许展示的战区白名单数组 `ALLOWED_WAR_ZONES`

### 2. `services/mockData.ts`
**修改位置**: 
- 第 104-140 行的战区提取逻辑（`ensureResourcesInitialized` 函数）
- 第 594-597 行的 `getStoresByRegion` 函数

**修改内容**:
- 在战区提取时，只保留第一至第六战区的数据
- 过滤掉不属于这六个战区的门店

### 3. `services/bmapService.ts`
**修改位置**: 第 43-51 行的 `REGION_COLORS` 颜色配置
**修改内容**:
- 移除 R0（职能部门）的颜色配置

### 4. 检查视图文件
检查以下视图文件，确保它们使用过滤后的 REGIONS 和 STORES：
- `views/Dashboard.tsx`
- `views/StoreOverview.tsx`
- `views/Benchmark.tsx`
- `views/BMapContainer.tsx`

## 实施步骤

1. **定义战区白名单**: 在 `dataAdapter.ts` 中定义允许展示的六个战区列表
2. **修改战区提取逻辑**: 在 `mockData.ts` 的 `ensureResourcesInitialized` 中，过滤战区数据
3. **修改门店过滤逻辑**: 确保只保留属于六个战区的门店
4. **更新颜色配置**: 移除职能部门的颜色配置
5. **验证视图文件**: 确保所有视图正确使用过滤后的数据

## 预期结果
- 所有页面只显示第一战区至第六战区
- 所有数据（包括报表、排行榜、趋势等）都只包含这六个战区的门店
- 职能部门的数据被完全过滤掉